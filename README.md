# Hocs-Helm-Kubectl
 
Docker image for CI/CD pipelines on DECS.

Contains Git, Curl, Drone CLI, Helm, Helmfile, Kubectl, psql

Published to https://quay.io/repository/ukhomeofficedigital/hocs-helm-kubectl
